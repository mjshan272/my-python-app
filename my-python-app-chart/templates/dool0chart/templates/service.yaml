apiVersion: v1
kind: Service
metadata:
  name: {{ include "dool0chart.fullname" . }}
  labels:
    {{- include "dool0chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dool0chart.selectorLabels" . | nindent 4 }}
