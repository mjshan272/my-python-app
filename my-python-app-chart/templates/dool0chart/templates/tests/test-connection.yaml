apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dool0chart.fullname" . }}-test-connection"
  labels:
    {{- include "dool0chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dool0chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
